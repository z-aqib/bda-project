version: "3.8"

services:
  superset:
    image: apache/superset:3.0.0
    container_name: superset
    ports:
      - "8088:8088"
    environment:
      SUPERSET_SECRET_KEY: supersecretkey
    networks:
      - bda-network

networks:
  bda-network:
    external: true
